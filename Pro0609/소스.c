#include<stdio.h>	//전처리기
//
//int f(int x) {
//	return (x * x + x);
//}

int main0() {
	int Age = 200;
	printf("전완기\n");
	printf("내 나이는 : %d 입니다 \n", Age+10);
	//printf("f(2)의 값은 %d\n",f(2));
	return 0;	// OS로 보내줌
}